workflows:
  android-release:
    name: CyberPulse Android Release
    triggering:
      events:
        - push
    scripts:
      - name: Get Flutter dependencies
        script: flutter pub get
      - name: Build APK Release
        script: flutter build apk --release
    artifacts:
      - build/app/outputs/apk/release/app-release.apk
